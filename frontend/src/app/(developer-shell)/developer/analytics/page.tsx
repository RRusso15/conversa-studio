import { PlaceholderWorkspace } from "@/components/developer/PlaceholderWorkspace";

export default function DeveloperAnalyticsPage() {
  return (
    <PlaceholderWorkspace
      title="Analytics"
      description="Track usage, outcomes, drop-offs, and quality indicators for deployed chatbot flows."
      highlight="Analytics is scaffolded now so the navigation and route architecture are stable from the MVP onward."
    />
  );
}
