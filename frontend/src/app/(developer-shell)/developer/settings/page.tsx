import { PlaceholderWorkspace } from "@/components/developer/PlaceholderWorkspace";

export default function DeveloperSettingsPage() {
  return (
    <PlaceholderWorkspace
      title="Settings"
      description="Manage workspace preferences, user-level configuration, and future API provider keys."
      highlight="Settings will later house account preferences and environment-level chatbot controls."
    />
  );
}
